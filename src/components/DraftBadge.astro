---
import TagPill from './TagPill.astro';

/*
  The draft badge, wherever a draft post surfaces (ADR 0021). It reads the
  entry's flag, never the environment; production filters drafts out before
  render. A badge, not a tag: no link, and no tag maps to rose.
*/
---

<TagPill tint="rose">draft</TagPill>
