<h2 class="section-title"><slot /></h2>

<style>
	.section-title {
		display: flex;
		align-items: center;
		gap: var(--space-4);
		color: var(--ink);
		font-family: var(--font-display);
		font-size: var(--text-section);
		font-weight: var(--weight-voice);
		line-height: var(--leading-heading);
	}

	/* The decorative rule to the right margin; a pseudo-element stays out
	   of the accessibility tree. The slotted label keeps shrink, so it wraps. */
	.section-title::after {
		content: '';
		flex: 1;
		height: 1px;
		margin-right: var(--space-2);
		background: var(--hairline);
	}
</style>
