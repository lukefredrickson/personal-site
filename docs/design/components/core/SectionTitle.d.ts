/** 23px/800 section heading with hairline rule to the right margin ("Where I've worked", "Writing"). */
export interface SectionTitleProps {
  children: React.ReactNode;
  style?: React.CSSProperties;
}
