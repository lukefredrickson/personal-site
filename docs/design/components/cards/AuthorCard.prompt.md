Sits at the end of every post, above prev/next. Photo placeholder is a neutral dot-matrix; the home link renders in `--link` (rose in both themes).

```jsx
<AuthorCard photoSrc="/img/luke.jpg" />
```
