Every image on the site lives in one of these — post heros (with caption), ride photos (captionless).

```jsx
<FramedFigure height={240} src="/img/summit.jpg" caption="top of the gap, mile 61" />
<FramedFigure height={130} tint="var(--tint-green)" placeholder="photo" />
```
