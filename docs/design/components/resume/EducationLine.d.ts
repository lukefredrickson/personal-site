/** One-line education mention below the career cards. */
export interface EducationLineProps {
  /** Rendered ALL-CAPS mono ("B.S. Computer Science"). */
  degree: string;
  school: string;
  year?: string;
}
