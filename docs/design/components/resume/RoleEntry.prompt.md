Promotion-ladder row. Newest role first with `current`; final row gets `last`.

```jsx
<RoleEntry current title="Senior Software Engineer" dates="2025–now"
  bullets={["Own front-end architecture across three product teams"]} />
```
