<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128">
    <!-- Placeholder mark: the logo's "L" plus the gold ".dev" dot. Adapts to
         dark browser chrome via the query below. -->
    <path class="mark" d="M40 28h18v56h34v16H40z" />
    <circle class="dot" cx="86" cy="42" r="11" />
    <style>
        .mark { fill: #55516f; }
        .dot { fill: #e1a035; }
        @media (prefers-color-scheme: dark) {
            .mark { fill: #dddceb; }
            .dot { fill: #ffc05a; }
        }
    </style>
</svg>
