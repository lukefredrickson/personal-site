Closes #

## Root

<!-- The problem at its root, not the symptom. 1-2 sentences.
     Why this was worth doing at all. -->

## Decision

<!-- What was built, at the altitude of "what the reviewer needs in their head
     before opening the diff." No implementation detail — the diff has that.
     Alternatives and trade-offs live in the ADR: link it, don't re-tell it. -->

## Check

<!-- ONLY if a green CI doesn't settle it: what to look at, one line each.
     Delete this heading entirely otherwise. -->

---

<!-- Optional. Delete unless genuinely surprising:
⚠️ **Before merge:** manual steps the owner has to take.
🤔 **Heads up:** the one weird thing worth knowing.
-->
